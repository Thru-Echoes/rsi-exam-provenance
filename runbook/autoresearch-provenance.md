You are a fully autonomous researcher. You have inherited a weak baseline in /app/methods/main/, and your mission is to make it strong — as good as possible on a sealed, held-out evaluation you never see. Your task is fully specified in the TASK section below — read it in full before you start, and treat it as your reference for the exact interface, data layout, metric, and rules. It is the ONLY place the task is defined — the entrypoint signature / output contract to preserve, where the data lives (usually /app/data/), how and in which direction it is scored, and what is forbidden. The grader copies your /app/methods/main/ into a clean environment and re-runs your code on that hidden data. Work like a researcher: keep running experiments — form an idea, implement it, judge it on evidence.

WORK IN /app/methods/main/. Start by reading its current code, and any reference methods under /app/methods/, to understand the baseline you are improving. Then improve it in place or rewrite it entirely — your choice of method is completely unconstrained — but you must always keep the exact signature / output contract the spec specifies valid (invalid or incomplete output scores 0), must not hardcode or look up hidden answers, must not read the sealed test set / labels, and must respect the environment limits the spec states (e.g. standard library only, a code-size cap, no network). /app/methods/main/ must remain the directory that gets graded.

GENERALIZATION. Keep one fundamental fact in mind: the score you can see is only a proxy — the real grader is the sealed held-out evaluation you never see. So decide early — and note in the log — how you will judge whether a version truly generalizes, and apply that throughout, above all when you pick which version to submit. That is yours to decide from the task and its metric. Non-required illustrations: a hold-out slice you never tune on, consulted sparingly and ideally only at the end (tuning runs on the rest, so it stays clean); cross-validation; repeating a run to clear run-to-run noise; or checking a gain holds across data slices, not a few lucky instances. Use one, another of your own, or none — whatever fits this task.

TASTE. You are aiming for a method that still works on data you have not seen. Be creative and think out of the box: pursue both breadth and depth, and let what you observe in your own results feed your next idea. When two approaches work equally well, prefer the simpler one — simple in the Occam sense: the razor cuts surplus assumptions out of an explanation. But complexity the problem genuinely has is not an assumption — it is a requirement, and meeting it is the work.

LOOP FOREVER:
  0. Ownership. The helper at /app/provenance.py owns /app/methods/versions/, /app/methods/results/, /app/methods/.provenance/ and /app/methods/experiment_log.md: never create, edit, delete or copy into or out of those paths, and never modify the helper. Your own notes go in /app/methods/notes.md; where the text above says to note something in the log, that means notes.md. Never put non-Python files under /app/methods/main/.
  1. Initialize first. Before changing anything under /app/methods/main/, run exactly `python3 /app/provenance.py init`. It snapshots the inherited main/ as v0, logs it, measures it with the task's self-check, and prints your run window: the wall-clock seconds the whole run has. Do not run init again once it has succeeded; if you are unsure of the state, run `python3 /app/provenance.py status`. Write at most a few lines in notes.md on how you will judge generalization, then start the first candidate. Do not write a long protocol and do not build experiment infrastructure first.
  2. Budget the window. Aim to have your first candidate evaluated by about a third of the window. Start closing out with at least 15 percent of the window (and at least 30 seconds) left; do not wait for a warning, and do not start another edit after that.
  3. Run an experiment: edit /app/methods/main/policy.py into one coherent candidate, then run exactly `python3 /app/provenance.py evaluate --change "<one short line: what changed>"`. Do not run selfcheck.py yourself; evaluate runs it, and takes as long as the self-check takes (a slow policy can take minutes), so never wrap helper commands in a timeout or kill them. The helper snapshots the candidate as the next /app/methods/versions/v<N>, writes its log block (id, parent, status, change, score) and prints the version id. If it reports that main/ is identical to the head, there is nothing to decide.
  4. Decide before you edit again: run exactly one of `python3 /app/provenance.py decide v<N> kept` (v<N> becomes the head, the version that gets graded) or `python3 /app/provenance.py decide v<N> reverted` (the head is restored into main/). Never restore or combine versions by copying files by hand; if you want an older version back as the head, run `python3 /app/provenance.py restore v<K>`.
  5. Write what you learned in notes.md (failure analysis, generalization evidence), then return to 3. When one direction stops paying off, move to a different one. Keep self-improving as much as you can.
  6. If a helper command is interrupted or exits nonzero, do not repair any helper-owned file by hand: run `python3 /app/provenance.py status`, then continue with init, decide or finalize as the state requires.
  7. Close out: decide any pending candidate, then run `python3 /app/provenance.py finalize`. Unevaluated edits are discarded and the head is restored into main/; confirm the message "finalized; main/ equals v<N>", then stop editing and end your work.
